#pragma once
#include <iostream>
#include <time.h>
#define TEMP template <class T>
#define re(i,n) for(int i=0;i<n;i++)
const int QueueSize = 10000;

TEMP
class queue
{
	int front, rear;
	T data;
public:
	
};